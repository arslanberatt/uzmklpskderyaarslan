<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Yönetici Paneli</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <div class="tab-pane fade show active" id="add-video" role="tabpanel" aria-labelledby="add-video-tab">
                <form id="addForm2">
                  <div class="form-group">
                    <label for="videoTitle">Başlık</label>
                    <input type="text" class="form-control" id="videoTitle" name="videoTitle" placeholder="Video başlığını girin">
                  </div>
                  <div class="form-group">
                    <label for="videoUrlEdit">İfream</label>
                    <input type="text" class="form-control" id="ifreamUrl" name="ifreamUrl" placeholder="Video İfream'ini girin">
                  </div>
                  <button type="submit" class="btn btn-primary mt-3">Ekle</button>
                  <a href="/video" class="btn btn-success ms-4">Videolara git</a>
                </form>
      </div>  
    </div>
  </div>
  <script>
    const addForm2 = document.getElementById('addForm2')

    const clearMessageForLogin= () =>{
      setTimeout(()=>{
        
      })
    }
    addForm2.addEventListener('submit', (e)=>{
      e.preventDefault();
            
      const formData = new FormData(addForm2)
      
      fetch('/videoAdd', {
        method:'post',
        body: formData
      }).then(response=>response.json())
      .then((res)=>{
        console.log(res)
      })
    })
  </script>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>