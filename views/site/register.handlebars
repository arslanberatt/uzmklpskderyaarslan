    <div class="container d-flex justify-content-center card p-5" style="height: 80%; width: fit-content; margin-top: 200px;">
        <div class="row">
            <div class="span12">
                <form class="form-horizontal" id="register" method="post">
                  <fieldset>
                    <div id="legend">
                      <legend class="">Login</legend>
                    </div>
                    <div class="control-group m-2">
                      <!-- Email -->
                      <label class="control-label"  for="registerMail">Email</label>
                      <div class="controls">
                        <input type="text" id="email" name="email" placeholder="" class="input-xlarge">
                      </div>
                    </div>
                    <div class="control-group m-2">
                      <!-- Username -->
                      <label class="control-label"  for="register">Username</label>
                      <div class="controls">
                        <input type="text" id="username" name="username" placeholder="" class="input-xlarge">
                      </div>
                    </div>
                    <div class="control-group ms-2">
                      <!-- Password-->
                      <label class="control-label" for="registerPassword">Password</label>
                      <div class="controls">
                        <input type="password" id="password" name="password" placeholder="" class="input-xlarge">
                      </div>
                    </div>
                    <div class="control-group m-2">
                      <!-- Button -->
                      <div class="controls">
                        <button class="btn btn-success" type="submit" >register</button>
                      </div>
                    </div>
                  </fieldset>
                </form>
            </div>
        </div>
    </div>
</body>

 <script>
    const register = document.getElementById('register')

    const clearMessageForLogin= () =>{
      setTimeout(()=>{
        
      })
    }
    
    register.addEventListener('submit', (e)=>{
      e.preventDefault();
      console.log('fadasdsad')
      
    
      const formData = new FormData(e.target)
      
    
      fetch('/register', {
        method:'post',
        body: formData
      }).then(response=>response.json())
      .then((res)=>{
        console.log(res)

      })

    })
  </script>